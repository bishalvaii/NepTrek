import React from 'react'

const BookingDetails = ({ destination }) => {
  return (
    <div>BookingDetails for {destination.name}

        <button>Working</button>
    </div>
  )
}

export default BookingDetails